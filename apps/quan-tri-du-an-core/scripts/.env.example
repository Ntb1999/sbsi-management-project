# Copy this file to scripts/.env (gitignored, never committed) and fill
# in your Jira FSS credentials. sync_jira_fss.py loads it automatically —
# just double-click SYNC_JIRA_NOW.bat, no need to `set` env vars by hand.
JIRA_USER=
JIRA_PASS=
